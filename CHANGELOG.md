# Changelog

One section per `v<semver>` tag. Mark public API changes as breaking or additive.

## Unreleased

- Initial setup as an XCFramework distribution repo (SPM binary targets, release script,
  verification workflow).
